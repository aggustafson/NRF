# BrightCo Next.js Head App - Environment Variables
# 
# This file should be copied to .env.local and modified with your environment variables
# to connect to your Sitecore XM Cloud instance.
#
# To get these values:
# 1. Go to XM Cloud Deploy Portal: https://portal.sitecorecloud.io
# 2. Select your Environment
# 3. Go to Developer Settings tab
# 4. Copy the values from the "Local Development" section

# =============================================================================
# REQUIRED: Sitecore Edge API Configuration
# =============================================================================
# The Context ID for connecting to XM Cloud Edge Platform
# This is required for the build process to generate site metadata
SITECORE_EDGE_CONTEXT_ID=

# Public Context ID for client-side API calls
# This should match SITECORE_EDGE_CONTEXT_ID for most cases
NEXT_PUBLIC_SITECORE_EDGE_CONTEXT_ID=

# Edge Platform URL (optional, defaults to https://edge-platform.sitecorecloud.io)
SITECORE_EDGE_URL=
NEXT_PUBLIC_SITECORE_EDGE_URL=

# =============================================================================
# REQUIRED: Site Configuration
# =============================================================================
# Your Sitecore site name (default: brightco)
NEXT_PUBLIC_DEFAULT_SITE_NAME=brightco

# Your default app language (default: en)
NEXT_PUBLIC_DEFAULT_LANGUAGE=en

# =============================================================================
# REQUIRED: Editing Mode Security
# =============================================================================
# Secret token to secure the Sitecore editor endpoint exposed by your Next.js app
# (/api/editing/render by default)
# This is required for editing mode to work
SITECORE_EDITING_SECRET=

# =============================================================================
# OPTIONAL: Local API Configuration (for local Sitecore instances)
# =============================================================================
# Use these if connecting to a local Sitecore instance instead of Edge Platform
# Your Sitecore API key for GraphQL endpoint authentication
NEXT_PUBLIC_SITECORE_API_KEY=

# Your Sitecore API hostname (e.g., https://<xmc_cm_host>.sitecorecloud.io)
NEXT_PUBLIC_SITECORE_API_HOST=

# =============================================================================
# OPTIONAL: Personalization Configuration
# =============================================================================
# Personalize scope (if using Sitecore Personalize)
NEXT_PUBLIC_PERSONALIZE_SCOPE=

# Personalize middleware timeout in milliseconds (default: 1000)
PERSONALIZE_MIDDLEWARE_EDGE_TIMEOUT=1000

# =============================================================================
# OPTIONAL: Debug Logging
# =============================================================================
# Sitecore Content SDK npm packages utilize the debug module for debug logging.
# https://www.npmjs.com/package/debug
# Set the DEBUG environment variable to 'content-sdk:*' to see all logs:
#DEBUG=content-sdk:*
# Or be selective and show for example only layout service logs:
#DEBUG=content-sdk:layout
# Or everything BUT layout service logs:
#DEBUG=content-sdk:*,-content-sdk:layout
